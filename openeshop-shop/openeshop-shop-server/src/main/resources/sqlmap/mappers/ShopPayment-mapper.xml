<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.anttribe.openeshop.shop.domain.ShopPayment">
	<cache readOnly="true" />
	<resultMap id="ShopPayment-resultMap" type="org.anttribe.openeshop.shop.domain.ShopPayment">
		<result property="id" column="id" />
		<result property="paymentId" column="payment_id" />
		<result property="config" column="config" />
		<result property="enabled" column="enabled" />
		<result property="shopId" column="shop_id" />
	</resultMap>
	
	<!-- select -->
	<sql id="select">
        <![CDATA[
            SELECT t.id, t.payment_id, t.config, t.enabled, t.shop_id
              FROM t_shop_payment_info t
        ]]>
	</sql>

	<!-- where -->
	<sql id="where">
		<where>
		    <if test="null != id and id != ''">
				AND t.id = #{id}
			</if>
		    <if test="null != paymentId and paymentId != ''">
				AND t.payment_id = #{paymentId}
			</if>
		    <if test="null != config and config != ''">
				AND t.config = #{config}
			</if>
		    <if test="null != enabled and enabled != ''">
				AND t.enabled = #{enabled}
			</if>
		    <if test="null != shopId and shopId != ''">
				AND t.shop_id = #{shopId}
			</if>
		</where>
	</sql>
	
	<!-- orderBy -->
	<sql id="orderBy">
	    <![CDATA[]]>
	</sql>
	
	<!-- limit -->
	<sql id="limit">
	    <if test="null != pagination">
			limit #{pagination.offset}, #{pagination.pagesize}
		</if>
	</sql>
	
	<!-- insert: 插入单条数据 -->
	<insert id="insert" parameterType="org.anttribe.openeshop.shop.domain.ShopPayment">
		<![CDATA[
		    INSERT INTO t_shop_payment_info(id, payment_id, config, enabled, shop_id)
		        VALUES(#{id}, #{paymentId}, #{config}, #{enabled}, #{shopId})
		]]>
	</insert>
	
	<!-- batchInsert: 批量插入数据 -->
	<insert id="batchInsert" parameterType="java.util.List">
		<![CDATA[
		    INSERT INTO t_shop_payment_info(id, payment_id, config, enabled, shop_id)
		        VALUES
		]]>
		<foreach collection="list" item="item" open="(" close=")" separator=", ">
			#{id}, 
			#{paymentId}, 
			#{config}, 
			#{enabled}, 
			#{shopId}
		</foreach>
	</insert>

	<!-- update: 更新单条数据 -->
	<update id="update" parameterType="org.anttribe.openeshop.shop.domain.ShopPayment">
        <![CDATA[
            UPDATE t_shop_payment_info t
        ]]>
		<set>
		    <if test="null != id and id != ''">
				t.id = #{id}
			</if>
		    <if test="null != paymentId and paymentId != ''">
				t.payment_id = #{paymentId}
			</if>
		    <if test="null != config and config != ''">
				t.config = #{config}
			</if>
		    <if test="null != enabled and enabled != ''">
				t.enabled = #{enabled}
			</if>
		    <if test="null != shopId and shopId != ''">
				t.shop_id = #{shopId}
			</if>
		</set>
		<include refid="where"></include>
	</update>
	
	<!-- batchUpdate: 批量更新数据 -->
	<update id="batchUpdate" parameterType="java.util.List">
		<![CDATA[
            UPDATE t_shop_payment_info t
        ]]>
		<trim prefix="set" suffixOverrides=",">
			<trim prefix="payment_id = case id" suffix="end,">
                <foreach collection="list" item="item">
                    <if test="null != paymentId and paymentId != ''">
                        when #{item.id} then #{paymentId}
                    </if>
                </foreach>
            </trim>
			<trim prefix="config = case id" suffix="end,">
                <foreach collection="list" item="item">
                    <if test="null != config and config != ''">
                        when #{item.id} then #{config}
                    </if>
                </foreach>
            </trim>
			<trim prefix="enabled = case id" suffix="end,">
                <foreach collection="list" item="item">
                    <if test="null != enabled and enabled != ''">
                        when #{item.id} then #{enabled}
                    </if>
                </foreach>
            </trim>
			<trim prefix="shop_id = case id" suffix="end,">
                <foreach collection="list" item="item">
                    <if test="null != shopId and shopId != ''">
                        when #{item.id} then #{shopId}
                    </if>
                </foreach>
            </trim>
        </trim>
		<![CDATA[
            WHERE id IN 
        ]]>
        <foreach collection="list" separator=", " item="item" open="(" close=")">
            #{item.id}
        </foreach>
	</update>

	<!-- delete: 删除单条数据 -->
	<delete id="delete" parameterType="org.anttribe.openeshop.shop.domain.ShopPayment">
	    <![CDATA[
            DELETE FROM t_shop_payment_info t
        ]]>
		<include refid="where"></include>
	</delete>
	
	<!-- queryAll: 查询所有数据 -->
	<select id="queryAll" resultMap="ShopPayment-resultMap">
		<include refid="select"></include>
		<include refid="orderBy"></include>
	</select>

	<!-- queryById: 根据id查询单条数据 -->
	<select id="queryById" resultMap="ShopPayment-resultMap">
		<include refid="select"></include>
		<include refid="where"></include>
	</select>

	<!-- queryByCriteria: 根据条件查询数据 -->
	<select id="queryByCriteria" resultMap="ShopPayment-resultMap"
		parameterType="Map">
		<include refid="select"></include>
		<include refid="where"></include>
		<include refid="orderBy"></include>
	</select>
	
	<!-- queryDataCount: 根据条件查询数据条数 -->
	<select id="queryDataCount" resultMap="int" parameterType="Map">
		<![CDATA[
            SELECT COUNT(1)
              FROM t_shop_payment_info t
        ]]>
		<include refid="where"></include>
	</select>
	
	<!-- queryByPagination: 根据分页条件查询数据 -->
	<select id="queryByPagination" resultMap="ShopPayment-resultMap"
		parameterType="Map">
		<include refid="select"></include>
		<include refid="where"></include>
		<include refid="orderBy"></include>
		<include refid="limit"></include>
	</select>
	
</mapper>